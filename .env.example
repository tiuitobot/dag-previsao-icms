# Copilot CLI normalmente usa auth própria (gh/cached session)
# Remova GITHUB_TOKEN antes de rodar steps copilot_cli para evitar conflito com PAT.
# Exemplo:
#   unset GITHUB_TOKEN
#   python scripts/run_pipeline.py --with-copilot

BCB_TIMEOUT=10
ICMS_N_SIMULACOES=1000
